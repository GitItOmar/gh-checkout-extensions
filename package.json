{
  "name": "checkout-b2b-root",
  "version": "1.0.0",
  "engines": {
    "node": "23.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd backend && npm install"
  }
}
